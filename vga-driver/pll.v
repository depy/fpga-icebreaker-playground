`timescale 1ns / 1ps

/*
25 Mhz clock - config values generated by running: icepll -i 12 -o 25.175

F_PLLIN:    12.000 MHz (given)
F_PLLOUT:   25.175 MHz (requested
F_PLLOUT:   25.125 MHz (achieved)

FEEDBACK: SIMPLE
F_PFD:   12.000 MHz
F_VCO:  804.000 MHz

DIVR:  0 (4'b0000)
DIVF: 66 (7'b1000010)
DIVQ:  5 (3'b101)

FILTER_RANGE: 1 (3'b001)
*/
module pll(
	input  clock_in,
	output clock_out,
	output locked
	);

SB_PLL40_PAD #(
		.FEEDBACK_PATH("SIMPLE"),
		.DIVR(4'b0000),		    // DIVR =  0
		.DIVF(7'b1000010),	    // DIVF = 66
		.DIVQ(3'b101),		    // DIVQ =  5
		.FILTER_RANGE(3'b001)   // FILTER_RANGE = 1
	) uut (
		.LOCK(locked),
		.RESETB(1'b1),
		.BYPASS(1'b0),
		.PACKAGEPIN(clock_in),
		.PLLOUTCORE(clock_out),
        // Unused pins but declared to keep the linter happy
        /* verilator lint_off PINCONNECTEMPTY */
        .PLLOUTGLOBAL(),
        .EXTFEEDBACK(1'b0),
        .DYNAMICDELAY(8'b0),
        .LATCHINPUTVALUE(1'b0),
        .SDI(1'b0),
        .SCLK(1'b0),
        .SDO()
        /* verilator lint_on PINCONNECTEMPTY */
		);

endmodule
